Given two binary strings, return their sum (also a binary string).

For example,
a = "11"
b = "1"
Return "100"

class Solution:
    def addBinary(self,a，b):
        a = int(a,2)
        b = int(b,2)
        return bin(a+b)[2:]
        
        
        
        
二进制加法与十进制加法转换，list切片
